<!DOCTYPE html>
<html>
<head>
    <title><%= h title %></title>

    <meta http-equiv="Content-type" content="text/html; charset=utf-8"/>

    <%= stylesheet_link_tag("themes/#{(current_user ? current_user.theme : 'default')}", :media => 'screen') %>
</head>

<body>
    <div id="navigation_menu">
        <a href="/">Home</a>
        <a href="/ocean">Ocean</a>
        <a href="/projects">Projects</a>
        <a href="/codes">Codes</a>

        <div class="addition">
            <a href="/flows/search!">Search</a>
        </div>
    </div>

    <div id="user_menu">
        <% if !current_user %>
            <span class="username">Anonymous</span>
            <a href="/login">Login</a>
            <a href="/register">Register</a>
        <% else %>
            <span class="username"><a href="<%= user_path current_user %>"><%= current_user.name %></a></span>
            <a href="/users/edit/<%= current_user.id %>">Edit</a>
            <a href="/logout">Logout</a>

            <div class="addition">
                <a href="/flows/following">Following</a>
            </div>
        <% end %>
    </div>

    <div id="header">
        <a style="text-decoration: none;" href="/ocean/new/flow">start flowing</a> ||
        <a style="text-decoration: none;" href="/codes/new">paste code</a>
    </div>

    <div id="container">
        <% flash.each do |key, value| %>
            <div class="flash <%= key %>"><%= value %></div>
        <% end %>

        <%= yield %>
    </div>

    <div id="footer">
        <div style="float: left; padding-left: 5px;"><%= Time.now.to_s %></div>
        <div style="float: right; padding-right: 5px;">You know the <a href="/rules">rules</a> and so do I. <a href="/about">About?</a></div>
    </div>
</body>
</html>
